# Evidence

Raw gate outputs captured during the 2026-08-18 run, exactly as the commands produced them
(only credential material was kept out of the capture path by design). This is the curated
set the docs cite — the full shell transcript and the uncited captures stayed on the runner.
File names match the gates in the docs and in `../reference/full-plan-and-results.md`.
